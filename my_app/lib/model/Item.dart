// Defines a class Item that represents an item in a shopping cart or catalog.
class Item {
  String name;
  double price;
  int quantity;
  
  // Constructor for creating an instance of Item.
  // This constructor initializes an Item with a name, price, and quantity.
  Item(this.name, this.price, this.quantity);
}